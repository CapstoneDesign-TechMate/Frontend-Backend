import React, { Component } from 'react';
import { View, Text, } from 'react-native';

export default class LedgerScreen extends Component {
    render(){
        return(
            <View>
                <Text style={{fontSize:30}}>Ledger Screen</Text>
            </View>
        );
    }
}